# Evalexpr
Evalexpr is a C program that evaluates arithmetic expressions given on standard input. It supports both infix notation and reverse polish notation, with proper operator precedence and error handling.
